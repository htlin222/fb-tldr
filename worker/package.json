{
  "name": "fb-tldr-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker proxy that summarizes Facebook posts via Workers AI, behind Cloudflare Access.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
